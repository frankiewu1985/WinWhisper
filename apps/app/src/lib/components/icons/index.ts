/** The following are custom icons that either
 * 1. Don't exist in lucide
 * 2. Do exist in lucide, but we prefer to override them with our own version from a different library
 */

export { default as ClipboardIcon } from './Clipboard.svelte';
